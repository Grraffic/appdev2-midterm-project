Admin area scaffold

This folder contains initial scaffold for the admin side of the application.

Files:

- `index.jsx` — exports admin routes/components (placeholder)
- `components/Sidebar.jsx` — left navigation for admin pages
- `pages/AdminDashboard.jsx` — admin dashboard page (starter)

Add additional admin pages and wire routes in `src/main.jsx` or your router when ready.
